Given an input, submit your answer for these tasks. {% for task in tasks %}
<task>{{ task.name }} : {{ task.description }}. {% if 'class' in task %} Possible labels: {% for class in task.classes %} 
<label>"{{ class.name }}" : {{ class.description }}</label> {% endfor %} {% endif %}</task> {% endfor %}
For example: {% for example in few_shot_examples %} 
"{{ example.input }}" : {{ example.label }} {% endfor %}
input: "{{ input }}"